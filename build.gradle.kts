plugins {
    id("aquamine.common")
}

allprojects {
    apply(plugin = "aquamine.common")

    group = "net.aquamine"
    version = "1.0"
    description = "Minecraft server implementation based on KryptonMC (AquaMine)"

}